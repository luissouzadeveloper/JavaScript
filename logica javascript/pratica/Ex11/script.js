// Escreva um programa que declare um array de suas frutas favoritas e imprima cada fruta na lista usando um loop.

let frutas = ['Maçã', 'Pêra', 'Banana', 'Morango'];
for(let i in frutas){
    document.write(`${i} ${frutas[i]}<br>`);
}