// Objeto Math
let n1 = 9
console.log(Math.sqrt(n1))
//let n2 = Math.floor(n1); // Math.floor arredonda para baixo
//let n2 = Math.ceil(n1) // Math.ceil arredonda para cima
let round = Math.round(n1) // Math.round arredonda para um valor mais próximo
//console.log(round);

//console.log(Math.max(1, 2, 3, 4, 5, -10, -50, 2000, 9, 11)) // Math.max() retorna o maior número de uma sequencia
//console.log(Math.min(1, 2, 3, 4, 5, -10, -50, 2000, 9, 11)) // Math.min() retorna o menor número de uma sequencia

const aleatorio = Math.round(Math.random() * (10 - 1) + 1) // Math.random() retorna um número aleatório entre 0 e 1.
//console.log(aleatorio)

//console.log(Math.pow(2, 10)) // Math.pow() retorna a potência de uma valor

console.log(0 / 0)
