// Método splice
const nomes = ['Maria', 'João', 'Eduardo', 'Gabriel', 'Júlia'];
// Remover
// const removidos = nomes.splice(-1, 1);

// shift
// const removidos = nomes.splice(0, 2);

// push
// nomes.splice(nomes.length, 0, 'Raimundo');

// unshift
nomes.splice(0, 0, 'Zé');
console.log(nomes);